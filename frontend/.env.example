# Port for local dev server (optional; default is 3000)
PORT=3000

# Backend API URL when using "local frontend + Docker backend":
# - Docker backend is exposed on host port 5001
# - Use http://localhost:5001/api/v1 (not 5000)
REACT_APP_API_URL=http://localhost:5001/api/v1
