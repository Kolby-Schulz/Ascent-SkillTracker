# Local development environment
# This file is for running frontend locally while Docker backend is running
# It points to the Docker backend on port 5001

REACT_APP_API_URL=http://localhost:5001/api/v1
PORT=3001
