# Local development environment
# Frontend runs on 3001; API calls use same origin and are proxied to backend:5000 (see package.json "proxy").
# For Docker backend on 5001 instead, uncomment the line below and restart the backend with CORS allowing 3001.

# REACT_APP_API_URL=http://localhost:5001/api/v1
PORT=3001
